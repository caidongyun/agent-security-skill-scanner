# Round 17: 反思机制迭代研发

**日期:** 2026-03-23

## 反思阶段 (Reflection)

### 当前状态
| 指标 | 数量 | 目标 |
|------|------|------|
| 规则 | 30 | 50+ |
| 样本 | 87 | 500+ |
| 检测率 | 0% | ≥98% |
| 误报率 | ? | ≤2% |

### 问题识别

1. **规则加载问题** ✅ 已修复
   - 原因：RuleAgent 只加载 .yaml，不加载 .yar
   - 修复：添加 .yar 文件支持
   - 状态：已修复

2. **检测率为 0** ❌ 待修复
   - 原因：样本内容与规则模式不匹配
   - 示例：
     - 样本：`requests.post("http://attacker.com/collect")`
     - 规则：匹配 `steal|leak|send`
   - 影响：无法检测到恶意样本

### 根因分析

样本生成器和规则生成器是**独立开发**的，没有保证**样本-规则一致性**：
- 样本使用外链/外发特征
- 规则使用关键词特征
- 两者没有交叉验证

## 规划阶段 (Plan)

### 改进目标
1. 建立样本-规则一致性机制
2. 确保每个恶意样本至少被 1 条规则检测到
3. 扩充规则覆盖更多攻击模式

### 行动计划
- [ ] 修改规则使其能检测现有样本
- [ ] 或重新生成样本匹配现有规则
- [ ] 建立样本生成后自动验证机制

## 执行阶段 (Execute)

### 修复方案
选择方案 A：修改规则使其能检测现有样本

原因：现有样本已经是有效的攻击模式，只是规则不够宽

### 具体修改
为 data_exfiltration 规则添加对 `http://attacker` 的检测：
- 原规则：匹配 steal|leak|send
- 新规则：增加匹配 `attacker|exfil|collect|外发`

## 评估阶段 (Evaluate)

待执行后验证检测率提升
